# JAKE - Environment Variables

# Database
# Format: mysql://user:password@localhost:3306/dbname
DATABASE_URL=mysql://root:@localhost:3306/phoenix_intel

# JWT Session Secret
# Generate a strong secret (at least 32 characters) for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Authentication Mode
# Currently only "local" is supported
AUTH_MODE=local

# Google Maps API Configuration
# Option 1: Direct Google Maps API (recommended)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
# Get your key at: https://console.cloud.google.com/
# Enable: Places API, Geocoding API

# Option 2: JAKE Proxy (if you have proxy credentials)
# BUILT_IN_FORGE_API_URL=https://forge-api.example.com
# BUILT_IN_FORGE_API_KEY=your-proxy-key

# If neither is set, the app will use mock data for development

# Client-side variables (must start with VITE_)
VITE_AUTH_MODE=local

# Performance & Logging Configuration
NODE_ENV=production
LOG_LEVEL=warn
# Options: debug, info, warn, error

# Server Configuration
PORT=3000
